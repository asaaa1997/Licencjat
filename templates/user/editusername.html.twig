{% include 'default/_form.html.twig' with {
    page_title: 'title_change_username',
    page_title_param: {'%id%': user.id},
    attr: {
        method: 'PUT',
        action: url('user_username', {'id': user.id}),
    },
    form: form,
    back_to_list_path: 'main_index',
} %}
